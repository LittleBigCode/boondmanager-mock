# Mock ↔ real BoondManager comparison

- real: `https://ui.boondmanager.com/api` | mock: `http://localhost:8000/api`
- structures only (keys, types, status codes) — no business value
- gap tolerated by construction: `isDeleted` (mock marker)

### Error dialect
- ✅ no authentication: real 401 `code+detail+status+title (isLogged=False)` / mock 401 `code+detail+status+title (isLogged=False)`
- ✅ invalid JWT: real 422 `code+detail+source+status (isLogged=False)` / mock 422 `code+detail+source+status (isLogged=False)`
- ✅ unknown profile id: real 404 `code+detail+status+title (isLogged=True)` / mock 404 `code+detail+status+title (isLogged=True)`
- ✅ unserved search (deliveries): real 405 `code+detail+status+title (isLogged=False)` / mock 405 `code+detail+status+title (isLogged=False)`

### Profiles
- `resources/{id}`: real 200 / mock 200 ✅ no difference
- `contracts/{id}`: real 200 / mock 200 ✅ no difference
- `resources/{id}/administrative`: real 200 / mock 200 ✅ no difference
- `resources/{id}/technical-data`: real 200 / mock 200 ✅ no difference

### /absences
- statuses: real 200 / mock 200
- ✅ no structural difference

### /actions
- statuses: real 200 / mock 200
- ✅ no structural difference

### /agencies
- statuses: real 200 / mock 200
- ✅ no structural difference

### /banking-transactions
- statuses: real 200 / mock 200
- ⚪ no data on this tenant — shapes not comparable

### /business-units
- statuses: real 200 / mock 200
- ✅ no structural difference

### /candidates
- statuses: real 200 / mock 200
- ✅ no structural difference

### /companies
- statuses: real 200 / mock 200
- ✅ no structural difference

### /contacts
- statuses: real 200 / mock 200
- ✅ no structural difference

### /expenses
- statuses: real 200 / mock 200
- ✅ conforming, documented gaps only:
  - attributes — documented gap (UNVERIFIED-FIELDS matrix): `delivery, numberOfKilometers, project, row`

### /invoices
- statuses: real 200 / mock 200
- ✅ no structural difference

### /opportunities
- statuses: real 200 / mock 200
- ✅ no structural difference

### /orders
- statuses: real 200 / mock 200
- ✅ conforming, documented gaps only:
  - attributes — documented gap (UNVERIFIED-FIELDS matrix): `creationDate, updateDate`

### /payments
- statuses: real 200 / mock 200
- ✅ no structural difference

### /poles
- statuses: real 200 / mock 200
- ✅ no structural difference

### /projects
- statuses: real 200 / mock 200
- ✅ no structural difference

### /purchases
- statuses: real 200 / mock 200
- ✅ no structural difference

### /resources
- statuses: real 200 / mock 200
- ✅ no structural difference

### /roles
- statuses: real 200 / mock 200
- ✅ no structural difference

### /times
- statuses: real 200 / mock 200
- ✅ no structural difference

### /times-reports
- statuses: real 200 / mock 200
- ✅ no structural difference

